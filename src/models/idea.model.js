class IdeaModel {
    constructor(id, datetime, title, desc, rating, category, details) {
        this.id = id;
        this.datetime = datetime;
        this.title = title;
        this.desc = desc;
        this.rating = rating;
        this.category = category;
        this.details = details;
    }
}

export default IdeaModel;
